# FlightCore 4.4.0 RC3

Immutable candidate `4.4.0-rc.3` / `20260826.111317-2cb17b2`.

This directory intentionally contains exactly five files. The canonical public install.sh is the self-contained manifest/hash-pinned bootstrap; it downloads and verifies the selected release archive before invoking the internal package installer with all companion files present.

RC3 is built from the exact immutable RC2 parent. Turning Distance is a stable heading-independent cruise range budget; Turn Home time is derived afterward to that same fixed boundary. RC2 Air Link, media, dual-stream, browser and flight-control behavior is retained.

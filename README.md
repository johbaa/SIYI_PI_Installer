# FlightCore 4.3.0 RC37

Immutable candidate `4.3.0-rc.37` / `20260822.132718-085b92b`.

This public directory intentionally contains exactly five files. Verify `FLIGHTCORE_RPI_INSTALLER_RELEASE_4.3.0-rc.37.sha256`, then use the resumable `install.sh`. RC37 replaces the RC26 unit-rendering wrappers with cached conversions integrated into the original HUD and telemetry render pass. The exact accepted RC36 parent is checksum-pinned; retained clean historical routes and fresh installation remain supported. Physical Android flight acceptance remains required.
